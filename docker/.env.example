# BlackRoad OS — local dev environment
# Copy to .env: cp .env.example .env
# Never commit .env — it contains secrets

NODE_ENV=development

# ─── Ports ────────────────────────────────────────────────────────────────────
GATEWAY_PORT=8787
REGISTRY_PORT=3001
OLLAMA_PORT=11434
WEB_PORT=3000

# ─── Gateway Auth ─────────────────────────────────────────────────────────────
# Token agents must send as Bearer token. "dev" allows local dev without auth.
BLACKROAD_GATEWAY_TOKEN=dev

# ─── Rate Limiting ────────────────────────────────────────────────────────────
RATE_LIMIT_RPM=60

# ─── AI Provider Keys (set only what you have) ────────────────────────────────
# These stay in the gateway — agents never see them
BLACKROAD_ANTHROPIC_API_KEY=
BLACKROAD_OPENAI_API_KEY=
BLACKROAD_GEMINI_API_KEY=
# Ollama runs locally — no key needed
